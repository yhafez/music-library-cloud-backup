{
	"watch": ["server"],
	"ext": "ts, js",
	"execMap": {
		"ts": "node --experimental-specifier-resolution=node --loader ts-node/esm -r tsconfig-paths/register"
	},
	"ignore": ["server/**/*.spec.ts"],
	"env": {
		"NODE_ENV": "development"
	}
}
